""" Plugins  --------------------------------
set surround
" set multiple-cursors
set commentary
set easymotion
set ReplaceWithRegister
set textobj-indent
set highlightedyank


""" Plugin settings -------------------------

""" Common settings -------------------------
set showmode
set ignorecase
set smartcase
set incsearch
set hlsearch
set scrolloff=10

""" Idea specific settings ------------------
set ideajoin
set ideaput

""" Mute bells
set visualbell
set noerrorbells

""" Map leader to space ---------------------
let mapleader=" "

""" Mappings --------------------------------
""" Leader Mappings
nmap <leader>o <Action>(FileStructurePopup)
nmap <leader>f <Action>(Find)
nmap <leader>r <Action>(Replace)
nmap <leader>F <Action>(FindInPath)
nmap <leader>R <Action>(ReplaceInPath)
nmap <leader>e <Action>(RecentFiles)<CR>
nmap <leader>p <Action>(RecentProjectListGroup)<CR>
nnoremap <leader>si :source ~/.ideavimrc<CR>

""" Copy & Paste Mappings
vmap <Enter> <Action>($Copy)
  workerIdleMemoryLimit: '1GB',

""" Move Mappings (Zed Like)
"" Zed Like
nmap gd <Action>(GotoDeclaration)
nmap gY <Action>(GotoTypeDeclaration)
nmap gI <Action>(GotoImplementation)
nmap gA <Action>(FindUsages)

nmap [c <Action>(VcsShowPrevChangeMarker)
nmap ]c <Action>(VcsShowNextChangeMarker)

"" Other
nmap g; <Action>(JumpToLastChange)
nmap g, <Action>(JumpToNextChange)
nmap ,u <Action>(GotoSuperMethod)

""" Other Mappings
nmap cd <Action>(RenameElement)

"" Not usufal
nmap gp <Action>(VcsShowPrevChangeMarker)
nmap gn <Action>(VcsShowNextChangeMarker)

""" Window mappings
nmap <C-w>o  <Action>(CloseAllEditorsButActive)

""" Emulate vim-visualstar
vnoremap * y <Esc>/<C-r>0<CR>

""" Skip register with x
nnoremap x "_x
vnoremap x "_x

" Reselect after indent
vnoremap < <gv
vnoremap > >gv
